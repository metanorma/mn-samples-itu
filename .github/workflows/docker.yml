# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
name: docker

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container: docker://metanorma/mn
    steps:
      - uses: actions/checkout@master
      - name: Checkout submodules
        shell: bash
        run: |
          auth_header="$(git config --local --get http.https://github.com/.extraheader)"
          git submodule sync --recursive
          git -c "http.extraheader=$auth_header" -c protocol.version=2 submodule update --init --force --recursive --depth=1
      - name: Bundle
        run: |
          bundle install --jobs 4 --retry 3
      - name: Build document in the Metanorma container
        run: |
          make clean all publish
      - uses: actions/upload-artifact@master
        with:
          name: published
          path: published

  deploy-gh-pages:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    needs: build
    steps:
    - uses: actions/checkout@master
    - uses: actions/download-artifact@master
      with:
        name: published
    - name: Deploy to GH Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GH_DEPLOY_KEY }}
        publish_dir: ./published
        force_orphan: true
        user_name: ${{ env.GITHUB_ACTOR }}
        user_email: ${{ format('{0}@users.noreply.github.com', env.GITHUB_ACTOR) }}
        commit_message: "${{ format('Deploy to GitHub Pages: {0}', env.GITHUB_SHA) }}"